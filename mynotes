to start up, run this from proj root: uvicorn src.api.app:app --reload

this launches the local backend server.

then launch ui.


To force kill the backend and all python processed (for example if the backend code is 'stale')
even after rerunning, then run this from proj root: Get-Process python | Stop-Process -Force
